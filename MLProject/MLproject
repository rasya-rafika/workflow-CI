name: adult-income-ci

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "../adult_preprocessing.csv"}
      target_col: {type: str, default: "income"}
    command: "python modelling.py --data_path {data_path} --target_col {target_col}"
